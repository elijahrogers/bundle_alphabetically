source "https://rubygems.org"
plugin 'bundle_alphabetically', path: '../../'

# This group is for testing
group :test do
  gem "minitest"
  gem "rspec", require: false

  # We use simplecov for coverage
  gem "simplecov",
    require: false
end

gem "bootsnap"
gem "rails"

# Don't require for boot
gem "rexml",
  ">= 3.4.2",
  require: false

group :development do
  gem "debug"
  gem "rubocop"
end
